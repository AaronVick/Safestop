{
  "name": "@safestop/core",
  "version": "0.1.0",
  "description": "Schema, validator, and types for Safestop governance artifacts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "schema"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "json-schema-to-typescript": "^14.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
